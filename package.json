{
  "name": "releaseshield",
  "private": true,
  "workspaces": [
    "apps/desktop",
    "services/local-ai",
    "packages/core"
  ],
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "npm run build --workspace apps/desktop && npm run build --workspace services/local-ai && npm run build --workspace packages/core",
    "lint": "npm run lint --workspace apps/desktop && npm run lint --workspace services/local-ai && npm run lint --workspace packages/core",
    "test": "npm run test --workspace packages/core"
  }
}
